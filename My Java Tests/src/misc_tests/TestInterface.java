package misc_tests;

public interface TestInterface {
	String interfaceString = "i am member/variable inside TestInterface";					// can omit 'public' in interface since they are implicitly already 'public'
	
	void intMethod1();									// can omit 'public' in interface since they are implicitly already 'public'
	void intMethod2();
}
